

#ifndef SIGPROC_COMMON_STRING_HPP
#define SIGPROC_COMMON_STRING_HPP

#include <string>

namespace sigproc {
    namespace common {

std::string replace(const std::string& str, const std::string& find, const std::string& replace);


    } // common
} // sigproc

#endif